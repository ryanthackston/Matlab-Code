
This should work if you just open up the 'decoderScript_SpectralPower.m' It calls on the other functions.

You might also need to change the filepath for the MAT files depending where you put decoding files and the MAT files.

